# Story 5-1: YAML Schema v2 -- Weight Profile Foundation

## Status: drafted
## Epic: Epic 5 -- Priority Scoring

## Intent
**Epic Handle:** "Your priorities reshape the scoreboard"
**Story Handle:** "This story builds the mixing board's memory by defining how slider positions are stored, validated, and migrated"

## Overview

As a user,
I want Archie to support a v2 YAML schema that includes weight profiles,
So that my priority settings can be saved and restored when importing/exporting architecture files.

**FRs:** FR69, FR70, FR71, NFR18, NFR19
**Size:** MEDIUM (4 pts) -- 4 tasks, ~22 subtasks, 7 files
**Risk Flags:** DATA_PIPELINE, ERROR_RESILIENCE, INPUT_SANITIZATION

## Acceptance Criteria

**AC-1: Schema Version Bump**
Given the schema version is bumped to "2.0.0",
When the app exports or imports YAML,
Then the schema_version field reads "2.0.0".

**AC-2: v1-to-v2 Migration (FR70)**
Given a v1 YAML file is imported,
When the importer detects a version mismatch (major version 1 vs app version 2),
Then it runs the "1" migration adding a default weight profile (all 7 categories = 1.0),
And the import succeeds with the migrated data passing v2 schema validation.

**AC-3: Weight Profile Validation (FR71, NFR18, NFR19)**
Given a v2 YAML file with a weight_profile field,
When the importer validates the file,
Then each weight must be a number between 0 and 1 (inclusive),
And the weight_profile must contain exactly the 7 known category IDs,
And invalid weight profiles are rejected with clear Zod error messages.

**AC-4: Missing Weight Profile Defaults**
Given a v2 YAML file WITHOUT a weight_profile field,
When the file is imported,
Then the weight profile defaults to equal weights (all 7 categories = 1.0),
And the import succeeds.

**AC-5: Strict Schema Enforcement**
Given ArchitectureFileSchema uses .strict(),
When v2 fields are added to the schema shape,
Then the schema uses .strict() on the v2 shape (no passthrough of unknown keys),
And the YAML variant applies the standard snake_case-to-camelCase transform for weight_profile.

## Architectural Acceptance Criteria

**AC-ARCH-LOC-1:** Weight profile types and constants added to `src/lib/constants.ts`
**AC-ARCH-LOC-2:** Schema v2 extension modifies `src/schemas/architectureFileSchema.ts` -- both camelCase and snake_case variants
**AC-ARCH-LOC-3:** Migration function registered in existing `MIGRATIONS` record
**AC-ARCH-LOC-4:** New types re-exported from `src/types/index.ts`
**AC-ARCH-PATTERN-1:** WeightProfile type is `Record<MetricCategoryId, number>`
**AC-ARCH-PATTERN-2:** Zod schema uses `z.object()` with one key per category (not `z.record()`) for precise validation errors
**AC-ARCH-PATTERN-3:** Migration function is pure `(data: unknown) => unknown` -- no side effects, testable in isolation
**AC-ARCH-PATTERN-4:** YAML variant uses `.transform()` for snake_case to camelCase -- consistent with existing pattern
**AC-ARCH-PATTERN-5:** weight_profile is optional in schema -- importer fills DEFAULT_WEIGHT_PROFILE when absent
**AC-ARCH-NO-1:** Do NOT use `z.record(z.string(), z.number())` -- allows arbitrary keys
**AC-ARCH-NO-2:** Do NOT add weight profile to Zustand store in this story -- that is Story 5-2
**AC-ARCH-NO-3:** Do NOT modify recalculationService or engine files -- this story is schema-only
**AC-ARCH-NO-4:** Do NOT use `z.passthrough()` or remove `.strict()`

## File Specification

| File | Path | Action |
|------|------|--------|
| constants | `src/lib/constants.ts` | MODIFY |
| architectureFileSchema | `src/schemas/architectureFileSchema.ts` | MODIFY |
| yamlImporter | `src/services/yamlImporter.ts` | MODIFY |
| yamlExporter | `src/services/yamlExporter.ts` | MODIFY |
| types/index | `src/types/index.ts` | MODIFY |
| schema v2 tests | `tests/unit/schemas/architectureFileSchema-v2.test.ts` | CREATE |
| migration tests | `tests/unit/services/yamlImporter-v2-migration.test.ts` | CREATE |

## Tasks / Subtasks

- [ ] Task 1: Define weight profile types and constants
  - [ ] 1.1 Add `WeightProfile` type as `Record<MetricCategoryId, number>` to constants.ts
  - [ ] 1.2 Add `DEFAULT_WEIGHT_PROFILE` constant: all 7 category IDs mapped to 1.0
  - [ ] 1.3 Add `WEIGHT_MIN = 0` and `WEIGHT_MAX = 1` constants
  - [ ] 1.4 Re-export `WeightProfile` from `src/types/index.ts`

- [ ] Task 2: Extend Zod schema to v2
  - [ ] 2.1 Add `WeightProfileSchema` as z.object() with one key per MetricCategoryId, each z.number().min(0).max(1)
  - [ ] 2.2 Add optional `weightProfile` field to ArchitectureFileSchema
  - [ ] 2.3 Add `weight_profile` with .transform() to YAML variant
  - [ ] 2.4 Bump `CURRENT_SCHEMA_VERSION` to "2.0.0"
  - [ ] 2.5 Test: reject unknown category IDs (.strict() on WeightProfileSchema)
  - [ ] 2.6 Test: reject out-of-range values

- [ ] Task 3: Implement v1-to-v2 migration
  - [ ] 3.1 Write migration function: adds weight_profile with defaults (snake_case, operates on raw YAML data)
  - [ ] 3.2 Register in MIGRATIONS record under key "1"
  - [ ] 3.3 Fix KNOWN GAP: re-validate migrated data against v2 schema
  - [ ] 3.4 Test: v1 input produces valid v2 output with default weights
  - [ ] 3.5 Test: migration preserves all existing v1 fields

- [ ] Task 4: Validation edge case tests (P1 hardening)
  - [ ] 4.1 Test: partial weight profile (missing categories) -> rejected
  - [ ] 4.2 Test: negative weight values -> rejected
  - [ ] 4.3 Test: NaN/Infinity -> rejected
  - [ ] 4.4 Test: boundary values (0.0 and 1.0) -> accepted
  - [ ] 4.5 Test: empty weight profile object {} -> rejected
  - [ ] 4.6 Test: extra unknown categories -> rejected
  - [ ] 4.7 Run `npm run test:quick` -- all tests pass

## Dev Notes

**Migration operates on raw YAML data (snake_case), BEFORE the Zod transform.** So it adds `weight_profile`, not `weightProfile`.

**Category IDs use kebab-case** (e.g., `operational-complexity`). Same in both YAML and TypeScript -- no transform needed for keys within weight_profile. Only the top-level field transforms: `weight_profile` (YAML) -> `weightProfile` (TS).

**Weight semantics:** MULTIPLICATIVE. Default=1.0 is identity (no change). Range 0-1. UI clamps minimum to 0.1 (Story 5-3), but schema accepts full 0-1 range. A weight of 0.0 means "completely ignore this category."

**checkSchemaVersion behavior:** With CURRENT_SCHEMA_VERSION bumped to "2.0.0", importing a v1 file returns `{ status: "migrate", migrationKey: 1 }`. The migration dispatch path already exists.
