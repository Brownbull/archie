# Story 6-5: E2E -- Constraint Guardrails Flow

## Status: done
## Epic: Epic 6 -- Constraint Guardrails

## Intent
**Epic Handle:** "Your red lines, visible on the canvas"
**Story Handle:** "This story validates the complete constraint guardrail experience end-to-end"

## Overview

As a developer,
I want E2E tests covering the constraint guardrail flow,
So that regressions in constraint CRUD, violation display, navigation, and YAML round-trip are caught automatically.

**FRs:** Validates FR51-FR57, FR71-FR72 end-to-end
**Size:** SMALL (2 pts) -- 2 tasks, ~11 subtasks, 2 files
**Risk Flags:** E2E_TESTING
**Depends on:** Story 6-3 (UI), Story 6-4 (export/import)

## Acceptance Criteria

**AC-1: Add Constraint and See Violation**
Given authenticated user with architecture loaded,
When E2E test adds a constraint via the constraint panel,
Then violation badge appears on affected canvas nodes.

**AC-2: Click Violation Navigates to Component**
Given constraint violations visible,
When clicking a violation in the dashboard list,
Then canvas pans to the offending component.

**AC-3: Remove Constraint Clears Violations**
Given active constraint with violations,
When constraint is removed,
Then violation badges disappear from canvas.

**AC-4: Export/Import Round-Trip Preserves Constraints**
Given constraints defined,
When architecture exported and reimported,
Then constraints list matches and violations are re-evaluated.

## Architectural Acceptance Criteria

**AC-ARCH-LOC-1:** Spec at `tests/e2e/constraint-guardrails.spec.ts`
**AC-ARCH-PATTERN-1:** SCREENSHOT_DIR constant, data-testid selectors, getByRole fallback
**AC-ARCH-PATTERN-2:** Auth pre-loaded from storageState
**AC-ARCH-PATTERN-3:** Screenshots to `test-results/constraint-guardrails/`
**AC-ARCH-NO-1:** No waitForTimeout > 1000ms
**AC-ARCH-NO-2:** No browser.newContext() for authenticated tests

## File Specification

| File | Path | Action |
|------|------|--------|
| E2E spec | `tests/e2e/constraint-guardrails.spec.ts` | CREATE |
| INDEX | `test-results/INDEX.md` | MODIFY |

## Tasks / Subtasks

- [x] Task 1: Constraint CRUD E2E
  - [x] 1.1 Define SCREENSHOT_DIR = "test-results/constraint-guardrails"
  - [x] 1.2 Load example architecture
  - [x] 1.3 Open dashboard overlay
  - [x] 1.4 Navigate to constraint panel via data-testid="constraint-panel"
  - [x] 1.5 Add constraint: Performance gte 10 (guaranteed violation)
  - [x] 1.6 Assert violation badge appears on canvas nodes (data-testid="constraint-violation-badge")
  - [x] 1.7 Screenshot: 01-constraint-violation.png
  - [x] 1.8 Click violation entry -> assert canvas navigates (overlay closes + inspector opens)
  - [x] 1.9 Remove constraint
  - [x] 1.10 Assert violation badges gone
  - [x] 1.11 Screenshot: 02-constraint-removed.png

- [x] Task 2: Export/import round-trip E2E
  - [x] 2.1 Add constraints (Cost Efficiency lte 4, Performance gte 7)
  - [x] 2.2 Export architecture, capture download
  - [x] 2.3 Clear canvas / fresh state (page.goto("/"))
  - [x] 2.4 Import exported YAML
  - [x] 2.5 Assert constraint panel shows both constraints (count=2, text contains category names)
  - [x] 2.6 Assert violation badges visible on canvas
  - [x] 2.7 Screenshot: 03-constraints-roundtrip.png
  - [x] 2.8 Update test-results/INDEX.md

## Dev Notes

**Constraint CRUD in Playwright:** Form interaction uses standard selectors -- category dropdown, operator dropdown, threshold dropdown, label input, submit button. All identifiable via data-testid attributes set in Story 6-3.

**File download:** Same pattern as Story 5-5 -- `page.waitForEvent('download')`, save to temp path, reimport.

**Threshold strategy:** AC-1/AC-3 uses `performance gte 10` (guaranteed violation -- no component averages exactly 10.0). AC-4 uses `cost-efficiency lte 4` + `performance gte 7` for round-trip validation.

**Native selects:** Category and operator dropdowns are native `<select>` elements (not shadcn), so `selectOption()` works directly.

## Senior Developer Review (ECC)

| Field | Value |
|-------|-------|
| Date | 2026-03-10 |
| Agents | code-reviewer (sonnet), tdd-guide (sonnet) |
| Classification | SIMPLE |
| Score | 8.3/10 |
| Outcome | APPROVE — 4 quick fixes applied |
| Action Items | 0 remaining (3 LOW deferred, no TD story needed) |

**Quick Fixes Applied:**
1. Removed unused `dump` import
2. Added `data-testid="constraint-violation-entry-{nodeId}"` to ConstraintPanel.tsx button + updated E2E selector
3. Strengthened AC-2 assertion (deselect node before violation click to avoid vacuous pass)
4. Made `openConstraintPanel` idempotent (guard with visibility check before toggle click)

**Deferred (LOW, no TD story):** field-level YAML assertions in AC-4, settling wait comment, screenshot between AC-2/AC-3

<!-- CITED: L2-006, L2-007 -->
<!-- INTENT: aligned -->
<!-- ORDERING: clean (step-08 skipped per O-007 trivial) -->
