# Story 6-3: Constraint Management UI & Canvas Violation Indicators

## Status: done
## Epic: Epic 6 -- Constraint Guardrails

## Intent
**Epic Handle:** "Your red lines, visible on the canvas"
**Story Handle:** "This story paints the red lines on the canvas and gives users the control panel to draw them"

## Overview

As a user,
I want to add/edit/remove constraint guardrails from the dashboard and see violations highlighted on the canvas,
So that I can define my requirements and immediately see which components breach them.

**FRs:** FR51, FR53, FR54, FR55, FR56
**Size:** MEDIUM (5 pts) -- 5 tasks, ~22 subtasks, 7 files
**Risk Flags:** PURE_COMPONENT, CROSS_STORE, ERROR_RESILIENCE, E2E_TESTING
**Depends on:** Story 6-1 (types), Story 6-2 (engine + store)

## Acceptance Criteria

**AC-1: Constraint Management Panel (FR51, FR56)**
Given the dashboard expanded overlay is open,
When viewing the constraints section,
Then I can add a new constraint (select category, operator, threshold, label),
And I can edit or remove existing constraints.

**AC-2: Canvas Violation Indicators (FR53)**
Given active constraint violations exist,
When viewing the canvas,
Then affected nodes show a violation badge (warning icon) distinct from heatmap colors,
And the badge shows violation count for that node.

**AC-3: Dashboard Constraint Status (FR54)**
Given constraints are active,
When viewing the dashboard,
Then an aggregate status shows: "N constraints, M violations" (or "All clear" when M=0).

**AC-4: Click-to-Navigate (FR55)**
Given a constraint violation is listed in the dashboard,
When I click the violation entry,
Then the canvas pans and zooms to the offending component,
And the component's inspector opens.

**AC-5: Overlay-Only Visual (FR53)**
Given constraint violation indicators on the canvas,
When violations are shown,
Then they are a SEPARATE visual layer from heatmap (badge/icon overlay, not color change),
And removing all constraints removes all violation indicators.

## Architectural Acceptance Criteria

**AC-ARCH-LOC-1:** ConstraintPanel component at `src/components/dashboard/ConstraintPanel.tsx`
**AC-ARCH-LOC-2:** ConstraintViolationBadge at `src/components/canvas/ConstraintViolationBadge.tsx`
**AC-ARCH-PATTERN-1:** ConstraintPanel reads store via selector `useArchitectureStore((s) => s.constraints)` -- not entire store
**AC-ARCH-PATTERN-2:** CRUD dispatches addConstraint/updateConstraint/removeConstraint -- does NOT import engine
**AC-ARCH-PATTERN-3:** ViolationBadge is a pure presentational component -- receives violation count as prop
**AC-ARCH-PATTERN-4:** Click-to-navigate uses existing canvas fitView/setCenter from React Flow + uiStore inspector open
**AC-ARCH-PATTERN-5:** ConstraintPanel in DashboardOverlay (expanded), not compact DashboardPanel
**AC-ARCH-PATTERN-6:** Compact DashboardPanel shows constraint status badge (count only)
**AC-ARCH-NO-1:** No engine imports in UI components
**AC-ARCH-NO-2:** No canvas color changes for constraints -- badges/icons only
**AC-ARCH-NO-3:** No constraint management controls in compact DashboardPanel

## File Specification

| File | Path | Action |
|------|------|--------|
| ConstraintPanel | `src/components/dashboard/ConstraintPanel.tsx` | CREATE |
| ConstraintViolationBadge | `src/components/canvas/ConstraintViolationBadge.tsx` | CREATE |
| DashboardOverlay | `src/components/dashboard/DashboardOverlay.tsx` | MODIFY |
| DashboardPanel | `src/components/dashboard/DashboardPanel.tsx` | MODIFY |
| ArchitectureNode | `src/components/canvas/ArchitectureNode.tsx` | MODIFY |
| ConstraintPanel test | `tests/unit/components/dashboard/ConstraintPanel.test.tsx` | CREATE |
| ViolationBadge test | `tests/unit/components/canvas/ConstraintViolationBadge.test.tsx` | CREATE |

## Tasks / Subtasks

- [x] Task 1: Create ConstraintPanel component
  - [x] 1.1 List existing constraints with category icon, operator label, threshold label
  - [x] 1.2 "Add Constraint" button opens inline form
  - [x] 1.3 Form: category dropdown (7 categories), operator dropdown (lte/gte), threshold number input (1-10, step 0.1), label text input
  - [x] 1.4 Edit button on each constraint opens inline edit
  - [x] 1.5 Delete button with confirmation (or undo pattern)
  - [x] 1.6 CRUD dispatches store actions (addConstraint, updateConstraint, removeConstraint)
  - [x] 1.7 data-testid="constraint-panel", "constraint-add-button", "constraint-item-{id}"

- [x] Task 2: Create ConstraintViolationBadge component
  - [x] 2.1 Renders warning icon (AlertTriangle from lucide-react) with violation count
  - [x] 2.2 Pure presentational: receives violationCount as prop
  - [x] 2.3 Positioned at top-right corner of node (absolute positioning within node)
  - [x] 2.4 Tooltip on hover: lists violated constraints
  - [x] 2.5 data-testid="constraint-violation-badge"
  - [x] 2.6 Empty state: does not render when violationCount === 0

- [x] Task 3: Integrate into dashboard
  - [x] 3.1 Add collapsible "Constraint Guardrails" section to DashboardOverlay
  - [x] 3.2 Show constraint status: "N active, M violations" or "No constraints defined"
  - [x] 3.3 Violation list with click-to-navigate: each violation shows node name + constraint description
  - [x] 3.4 data-testid="constraint-status", "constraint-violation-list"
  - [x] 3.5 Compact DashboardPanel: show small badge with violation count when > 0

- [x] Task 4: Canvas integration
  - [x] 4.1 ArchitectureNode reads violations for its nodeId from store
  - [x] 4.2 Renders ConstraintViolationBadge when violations exist
  - [x] 4.3 Click on badge opens DashboardOverlay constraint section (or inspector)

- [x] Task 5: Unit tests
  - [x] 5.1 Test: ConstraintPanel renders empty state with add button
  - [x] 5.2 Test: ConstraintPanel renders constraint list
  - [x] 5.3 Test: Add constraint dispatches store action
  - [x] 5.4 Test: Delete constraint dispatches store action
  - [x] 5.5 Test: ViolationBadge renders with count
  - [x] 5.6 Test: ViolationBadge does not render when count is 0
  - [x] 5.7 Test: Click violation navigates to component
  - [x] 5.8 Test: Constraint status shows correct counts
  - [x] 5.9 Run `npm run test:quick` -- all pass

## Dev Notes

**Click-to-navigate pattern:** React Flow provides `fitView({ nodes: [nodeId] })` and `setCenter(x, y, { zoom })`. Use the node's position to center. Then dispatch uiStore action to open inspector for that node.

**Badge positioning:** Use CSS `position: absolute; top: -8px; right: -8px;` within the node wrapper. React Flow custom nodes support arbitrary React content.

**Form UX:** Inline form within the panel (not a modal). Category dropdown populated from METRIC_CATEGORIES. Threshold dropdown shows "Low (1) / Medium (2) / High (3)". Label is optional -- auto-generate "{Category} {operator} {threshold}" if empty.

## Tech Debt Tracking

| TD Story | Description | Priority | Action |
|----------|-------------|----------|--------|
| TD-6-3a | Extract renderForm + optimize ArchieNode store subscriptions | MEDIUM | CREATED |

## Senior Developer Review (ECC)

- **Date:** 2026-03-10
- **Agents:** code-reviewer (sonnet), security-reviewer (sonnet)
- **Classification:** STANDARD
- **Score:** 8.5/10
- **Outcome:** APPROVE — 6 quick fixes applied, 1 TD story created (TD-6-3a)
- **Quick fixes:** threshold bounds enforcement, label length enforcement, operator/category allowlist validation, unsafe cast removal, key index cleanup, selector optimization
- **Cost:** $8.61

<!-- CITED: L2-001, L2-005 -->
