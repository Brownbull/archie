# Story 7.5-2: Pathway Suggestions Hook & Store Integration

## Status: ready-for-dev
## Epic: Epic 7.5 -- Weighted Pathway Guidance

## Intent
**Epic Handle:** "Show the roads, not just the dot"
**Story Handle:** "This story wires the pathway engine to the store so the UI can read suggestions reactively"

## Overview

As a user,
I want pathway suggestions to recompute automatically when I change weights, constraints, components, or data context,
So that the suggestions always reflect my current architecture state.

**FRs:** FR73, FR74, FR75, FR76, FR77
**Size:** SMALL (3 pts) -- 3 tasks, ~10 subtasks, 3 files
**Risk Flags:** CROSS_STORE
**Depends on:** Story 7.5-1 (pathwayEngine.ts must exist)

## Acceptance Criteria

**AC-1: Reactive Computation**
Given the architecture store state (tier, weights, constraints, data context items, nodes),
When any of these change,
Then the `usePathwaySuggestions` hook recomputes suggestions via `computePathwaySuggestions`.

**AC-2: No New Store State**
Given pathway suggestions are derived data,
When computed,
Then they live in a `useMemo` inside the hook -- not as new fields in `architectureStore`.

**AC-3: Input Assembly**
Given the hook reads from `architectureStore` and `componentLibrary`,
When assembling inputs for `computePathwaySuggestions`,
Then it passes: tier gaps, next tier requirements, all components, existing node categories, existing component IDs, weight profile, constraints, data context items, and category scores.

**AC-4: Empty State**
Given no tier evaluation (no components on canvas) or max tier achieved,
When the hook runs,
Then it returns an empty suggestion array (not null, not undefined).

## Architectural Acceptance Criteria

**AC-ARCH-LOC-1:** Hook in `src/hooks/usePathwaySuggestions.ts` (CREATE)
**AC-ARCH-PATTERN-1:** Hook uses `useMemo` over `computePathwaySuggestions` -- no debounce, no worker
**AC-ARCH-PATTERN-2:** Hook reads from `architectureStore` via selectors (useShallow where needed)
**AC-ARCH-PATTERN-3:** `componentLibrary.getAllComponents()` called outside useMemo (service is not reactive)
**AC-ARCH-NO-1:** No new state fields added to architectureStore
**AC-ARCH-NO-2:** No side effects in the hook -- pure derivation only

## File Specification

| File | Path | Action |
|------|------|--------|
| usePathwaySuggestions | `src/hooks/usePathwaySuggestions.ts` | CREATE |
| usePathwaySuggestions tests | `tests/unit/hooks/usePathwaySuggestions.test.ts` | CREATE |
| types/index | `src/types/index.ts` | MODIFY (re-export hook return type if needed) |

## Tasks / Subtasks

- [ ] Task 1: Create usePathwaySuggestions hook
  - [ ] 1.1 Read currentTier from architectureStore
  - [ ] 1.2 Read weightProfile, constraints, dataContextItems, nodes, computedMetrics from store
  - [ ] 1.3 Derive existingNodeCategories and existingNodeComponentIds from nodes
  - [ ] 1.4 Get allComponents from componentLibrary (non-reactive, outside useMemo)
  - [ ] 1.5 Compute next tier requirements from tierDefinitions (match currentTier.tierIndex + 1)
  - [ ] 1.6 Call computePathwaySuggestions inside useMemo with all inputs as dependencies
  - [ ] 1.7 Return { suggestions: PathwaySuggestion[], hasGaps: boolean, nextTierName: string | null }

- [ ] Task 2: Handle edge cases
  - [ ] 2.1 currentTier is null -> return empty
  - [ ] 2.2 isMaxTier -> return empty (read from currentTier.isMaxTier in TierResult, do NOT recompute)
  - [ ] 2.3 componentLibrary not initialized -> return empty (guard)

- [ ] Task 3: Tests
  - [ ] 3.1 Test: hook returns suggestions when tier gaps exist
  - [ ] 3.2 Test: hook returns empty when no components on canvas
  - [ ] 3.3 Test: hook returns empty at max tier
  - [ ] 3.4 Test: suggestions update when weight profile changes (mock store state)
  - [ ] 3.5 Test: suggestions update when constraints change
  - [ ] 3.6 Run `npm run test:quick` -- all pass

## Dev Notes

**Pattern reference:** Look at `useDashboardWeights.ts` for the existing hook + useMemo pattern over store selectors.

**Tier definitions access:** `DEFAULT_TIER_DEFINITIONS` from `src/lib/tierDefinitions.ts`. To find the next tier's requirements, filter by `index === currentTier.tierIndex + 1`.
