There are too many places to call to action for adjustment. For example:
- In the bottom bar, I'm adjusting in Quest 4 on DNS routing and I see that the foundation has 10 suggestions.
- If I go all the way to the far right part of the bottom bar, I can see the architectural scores. There are different scores there but if I click them nothing happens, so I don't really know what is best: to have one component or another component.
- I don't know the trace for each one of the points there, like performance, reliability, scalability, and so on.
- For path guidance, which I think is the thing that is showing in the foundation, we have so many things to add into the diagram. Although we have blocked almost all of them, it's like you are showing stuff that is not currently available.
- In the quest itself, in the top left corner, it is saying to me what I should do on each step throughout the quest. I'm not sure if I want to be hand-held that hard on the quest itself. I think that's part of the hints part. If it's a checking, like a compiler thing that tells you, "Okay, this is not running because of this," that might be ok.
- Let me check too. In Quest Mode it says: "Tackle add compute block. I am in observed baseline. I am the compute block, so I add the block; then it says, "Add a monitoring block." Yeah, it's telling me, step by step, what I must do, and that is not the best way to do this. I shouldn't have that hard of hints, like clues about what to do.
- It's ok if we have a syntax compiler in the sense that syntax for architectures or architecture tries to let say build and it's not building because there is some fundamental thing, something's wrong, or a kind of connection is not well connected. That kind of thing is okay, but hand-holding I don't even have to think here; that's the thing.
- Also, the components I cannot modify. I am on observe baseline quest, which is like the fifth of six ones, and I cannot modify any option on the blocks. Is that expected? If it is, that's okay, but if not, we should fix it.
- The way that we write our challenges should give a little bit more context of what we are trying to achieve, like what is the actual problem, and put ourselves in an actual issue, like you're in a company and you're trying to do this and that, and the company has this demand and this needs latency and availability. Like telling a story behind the challenge so it's not only a description of what needs to be done.
- Also, the description of the challenges or the quests. For example, in "Identity Yourself: Add an Out There Upstream of Compute". That is straightforward; what we need to do. There is no study behind, no reason, no context building of why we need this. Also, we are telling the developer just try away what it needs to do, like if you know what is a compute, which is suggested later that compute plot. Later we saw in compute that we have only one block, and then add out security before that or upstream that. That's all that it needs to be done in this challenge, so there is not really a challenge; it's just following instructions. 
- In the authentication challenge, if I put the node for authentication, then a node for compute, then I connect the traffic source HTTP out to the HTTP in and out, and then, in out, I connect the out out, the authentication out to the HTTP in in the compute. I get the icon that there is a compatibility warning: port type mismatch, authentication against HTTP, but if I run the simulation, I still get three stars. Is that expected, or should we enforce port-to-port connections? 
- Also, I am seeing that the kind of traffic, based on the port that we are connecting to, determines the color of the dots that are being transported through the link between two blocks. The color seems to be associated with the throughput.
I am thinking how we can represent all the dimensions in a link between two blocks:
- The dots that are being transported
- The color of the line (the line itself can be a dashed line, a dotted line, or a long-dashed line, something like that)
- Whether the line is glowing or has an aura, because I would like to represent not only the kind of flow that goes there (which is very good), but also the throughput (how many packets can go). If we have better latency, it should have more packets being transported, and I guess that the best representation for that should be the number of dots that are being transported. If we have more, they should go faster in order to go through the line, so if we have more, that means that the line can handle more.
We also like to represent the protocol (HTTP, UDP, HTTPS, and so on) somehow, the throughput, the kind of traffic, just like we have today, where authentication transports orange dots and HTTP transport blue dots, and so on. That's okay. The compatibility, I think that we are representing that with the colors of the lines currently.
- Also, when we finish a quest, we should have in that the prop up that says the stars and all the statistics. When we get the three stars and we unlock the different pieces to modify (for example, the traffic source), we should also invite the user to somehow, with a button or one of the buttons should be changed to an invitation to continue experimenting and tailor the demand to stress the system. Try with different demand or something like that. Right now, it's just complete quests and then very great options or continue, so pick another quest and that's it. But there is also the possibility of continuing on the same quest with the same diagram, change the input demand and see if the architecture breaks at some point. Or actually, we can ask them to break it. Go now and break it, and that could unlock a hidden star, one more star for when we identify when the system breaks. Yeah, that could be another star, and the way that we want it to happen is through modifying the traffic source, not modifying the actual architecture, but only the traffic source. If we have more than one, at least one of them should be modified so we know this when the architecture breaks 
- I am doing the async pipeline quest now, and it seems like we are missing a couple of challenges, because I just had, until this point, used DNS database monitoring and compute. Right now I have Messaging Load Balancer and cache as three new components at once. The challenge is good; we should skip the challenge, but I think that we are missing some steps or challenges or quests where we show how to use Messaging Load Balancer caching. 
- Also, there are some icons that we don't have for traffic. For example, we have a square with a background, so let's make sure that all our options across all blocks have corresponding icons and icons for the two modes: the pixel live mode and the official mode. Pixel live still has a lot of icon generation to spend on 
- On simulation, when I hit "Replay" after modifying the architecture, I don't get the grade. We should have an option, maybe in the center of the title of the simulation bar, to rerun the simulation instead of replay. We have two buttons for replay in the bottom right corner of the simulation section, so to rerun the simulation and get results, I have to close it, do a start challenge again, and then I get results, but we should be able to get results by some button in the simulation section 
- In the async pipeline quest, I got the three stars by connecting:
  - traffic source to a load balancer
  - the load balancer to compute
  - compute to a cache
  - compute to a message queue
  - the message queue to a SQL database
That went right. I got three stars, but the connection between the compute and the message queue was from HTTP out on compute to a stream in in message queue, and there I have a broken connection, like a compatibility warning. The same happens from the stream out on message queue to the SQL database. Database import I also have an AMQP connection compatibility warning. It seems like we are missing some ports somewhere, because I didn't find the port for the stream in or stream out on the other components. Also again, we have the same issue that we can connect incompatible ports, but the simulation still runs ok.
- Also, when I unlock a certain icon, like for example I'll unlock it, I was on the builder icon and then unlock the apprentice rank 2 icon based on experience. We have icons also in the disciplines; we should inform that somehow, may be in the results section of the simulations once we get that about the new icons that we have available. Also, we have to recalculate and recreate more icons for all the disciplines because all of them have, or at least some of them have, more challenges. For example, foundations, data, and reliability, and ops have more challenges, so we should have more icons created with the logic that we have before. Basically, the logic is: we take the total number of icons, we divide by two, we round up down, and then we put the last icon in the last section. From that point forward, we start putting icons. So, for example, in foundations, we have eleven challenges, so half of it is 5.5, we round up down, and we end up with five icons. In the 11th challenge on the 11th level, four foundations, we should have the last icon, and then have that be the first of five. Then this second one should be on level nine, then the third one, level five, the fourth one, and level three, the fifth one 
- I rebelled. I used all the hints in the data pipeline quest, but still I am not able to achieve the desired results. From them, the third one, for example, didn't actually give me any instructions on what to modify to get better results. The fifth one didn't really allow me to reach the good results, so I guess that we have to fix that and check all the other hints and other challenges to allow the users to actually have to spend stars on hints and get value from them.
- Also, on this one, it is suggested to have replicas or instances, but there is no load balancer yet on this challenge, so there is an inconsistency there. It requested that some replicas, but no load balancer is allowed here. What I tried was to connect traffic source to MSSQL, to the compute unit of object storage and an ETL pipeline, and connect to the ETL and LLM gateway. I haven't been able to solve this. Maybe I'm wrong, but reading the hints and trying different architectures have not allowed me to get the three stars yet. I'm not sure if this is well configured or if it is my issue. I would like to have the solution for this one. 
- Also, the test conditions for different data corruption, database failure, network partition, and so on, should also be blocked until we reach the three stars. Once we reach them and we get the challenge to break it, we should have an indicator there, like it says you can try this also, but to get the three stars, the additional fourth star, we should break the system only with traffic stress. The other test conditions are just to explore different reasons to break this, like different got different reasons that might break this system. The challenges to restore also are based on the test conditions available that we have. What test conditions break the system? Kind of highlight or make them glow so the user can experiment with them and see that those break the system.
- Also, for the fourth star, we want to get that one by modifying our traffic sources, which should also start in the challenge. For each option of the traffic source, for the peak, like a request per second, or the section for realistic, or the section for read and write, or the section for multi-region. For all those four sections, which one could break the system by itself? The idea is that the user has to modify just one of them at a time, so we could break the system by having multi-region, or break the system by changing to a read-heavy approach, or break the system by changing it to a big approach, or break the system just by changing the request per second, right?
We should store in each one of them how we can break the system, and once we reach the point where we are being asked to break the system, we should be able to reach each one of these conditions. For example, on pick, if we go to the pick or request per second that actually breaks the system, we will get a pop-up: okay, nice, you broke the system with RPS. If we have another configuration that breaks the system, we should say now try with other attributes that also might break the system for themselves, and there should be a button like okay, let's try it, or just cancel and continue. If we press the okay, let's try it, the configuration for the traffic source should go back to the default, and that would allow the user to test the system to break it using another option in the traffic source.
The way that the verification that we should do is that the traffic source the user only changed one of these to break it. If that happened, then we can mark that as done, like:
  - okay, it already broke it by modifying only the request per second
  - it broke it by modifying only the kind of flow, the kind of request that we were going to get, like realistic or other
  - we broke it only by modifying the kind of write or read that we had in the system
  - we just broke it by changing region to region
That's the idea, not a combination of them, just one at a time, and the user should configure each one of those to get the 4th-star.
  - Actually, we can think in a way that we can give additional stats by making the challenge resilient to test conditions like:
  - data corruption
  - database failure
  - network partition
  - region outage
  - single node failure
  - traffic spike
This is not necessary for this phase, but maybe the challenge can have a way that some test conditions might break the system. If we can select those test conditions and then make the system resilient to that, that can earn an extra star. It might not be necessary that all the test conditions are covered for all the challenges, but some of them might be interesting to have in some challenges and then solve them to learn about how to prepare a given system for that condition. This is not for all the challenges, so it would be interesting to have a couple of challenges for that.

If we have those kinds of challenges, I would like to have in the quest log or the quest menu the challenges that have these additional test conditions to have some kind of indicator, like an icon in the top right or top left or bottom right corner, that says that there is an additional challenge. Once we solve that, or actually, once we fix it, we have a status for the quest. When we complete them, we show them with the three steps in the center right, and those three stats can be a little bit bigger.

If we fix and if we accomplish also the additional challenges, like breaking the system with the input source conditions, all the input source conditions required for the system, that is an additional star that we can show in the quest log. For these other challenges, the star for breaking the system with traffic source should be a different color from the normal stats, and to make the system reliable given different test conditions, all the test conditions should be different color stars. Those stars should show in the quest log only as a star with a different color and a number indicator of how many of those stars we get in that given challenge. 

And actually, these two other kinds of stars, only the star that is for breaking the system using the traffic source will be added to the pool of stars for the hints. All the other stars for the other challenges will be other kinds of currency. Actually, we can make it not be a star but something else, and we will design later in the future what we will do with that currency. That's another kind of currency; it's a more expert kind of currency, so we have to think about what we can unlock with that currency later. 
- Also, in the quest log, all the colors have significance, right? They have meaning; they are related to some discipline. So we have to put that legend about what color is what somewhere. In the left section, below the quest log title, we can have there the colors and the disciplines and what the colors represent. 
- On the Quest description that we have in the Quest Log on the right side, if we add these extra challenges for the test conditions or the input source condition, we can also describe them in that section, like extra challenges and details, and have details about that. 
- I haven't tested this yet, but the button that allows us to save a given custom configuration on any block, on the traffic source, that button shouldn't. If we save something, that shouldn't apply to any quest in the future, because that is determined by the challenge, right? For all the others, it's the same, but for the traffic source we shouldn't be able to save that. Actually, we can disable this button for the traffic source at all, even in free mode. It's okay that the user has to tailor its traffic source for each case, but for all the other kinds of blocks we should have saved their configuration if we save anything. 
- I am doing tier 2 challenges, and I am on the one called Scale Out. Just here we have the load balancer explained through, but before that I had to tackle the async pipeline one in tier one, which required a lot of work. We have already had to use the load balancer in other challenges, but this one, the Scale Out, seems to be like the tipping point that should allow us to unlock the load balancer itself. It seems like it is. I don't get why the load balancer is available in challenges before this one. This basically surfaces an issue where some challenges use components that are not unlocked yet, so let's verify also that this doesn't happen for all the other blocks. Unless we have to fix the challenge and the dependencies, we can have even more tiers if it is necessary. I would like to stick with the ones that we have, but we can have more. What we can also have is more levels, like more rows per tier, that allow us to have more co-dependencies between challenges. That way we don't have to have challenges that use components that are not unlocked yet. That way we can ensure that we will have challenges that go in a progressive way, in the sense that any given block has its own unlock challenge, and after that then we can use that in other ones, but not before. 

- I am on the Burst App Server challenge.

For the traffic source I connect it to a load balancer, then to a compute, then to a message queue, then to a worker. I put in compute FastAPI with 2x replicas, with uvicorn single process. Then I went to the worker and selected Celery, and the strategy changed from Prefork to Eventlet IO bound.

We did not solve the challenge, but one thing that I did not get is: OK, I changed from Prefork to Eventlet, and I can see the differences in economics, but I don't know what that is. What is the actual change? For the vendor we have the link to see the vendor, which is OK, but for the tier between Prefork and Eventlet there is no link or anything. I would like to have the link and some sort of little explanation of what the different tiers are doing, because the tier is changing just the economics; it's not describing something else. A little description should be required in place.

For the tier we are selecting, for example in the worker section, if we select Sidekick, we have Enterprise and Standard. There should be a little description somewhere that tells us the difference between one and the other. In economics we are already giving that information about cost, throughput, and latency, but there should also be a little description of what kind of thing is changing, which is pricing with the compute power or the static IP in the backend code, something like that, but a very short description about it. 

- Also, I think that we are missing some scaling challenges, so we can transform some of the ones that we have or create new ones. The idea is the following: for some challenges, we will create a structure from the ground up. What we will have is nothing, a blank canvas. Traffic source, that's it.

Traffic source configured with the demand required, or traffic sources, more than one. We may have some cases which are more close to the reality, where you already have an infrastructure in place and you want to expand it, add new capabilities, new load or demands infrastructure, or change it because of a new use case. For those, we might have already wired an architecture in place, an architecture that, based on the new requirements, is not the one that we want.

That's something else I want to implement, like challenges that already have something in place that we have to scale up somehow. Add new blocks or replicas or configurations to make it work based on the new requirements. That's something else that we should have in our availlble quests in the quest log. for exapmle Stream the Data challenge starts with "Switch from fire-and-forget queues to a durable event log...." this suggest an architecture already  in place that needs  some changes.

- also on quest Observe to Recover we can see that we have a situation where a component gets broken mid-infrastructure. So, on this case, we probably will need to have the architecture in place and put what component fails. Maybe the test condition can be on a block basis, and we can say which component may fail instead of having it as a general condition. Because, as a general, we don't have visibility of where it is failing. Some test conditions might be general, and some others might be specific to specific blocks. Actually, on this specific challenge, I don't get why changing the observability provider solves the equation. I changed to TATA_DOC, and it solved everything, but I don't get why. What would be the factors that could clarify this? 

- I am on the quota keeper quest, and again the pattern in which we just do what is describing the challenge is surfacing again. I just did what it suggested, and I solved the challenge without having to tune anything or understand anything. Again, the description of the challenge shouldn't be an instruction of what to do; it should be like the situation, context, and what things to handle in the architecture, but not the actual solution.

The hints can be progressively different hints about what is the solution, and we might change the hint system. Instead of having five hints for each challenge, we can have up to five, but from one to five hints we can have on any given challenge. That way we have actual hints that make sense based on the difficulty of the challenge. 

- Actually, building on top of the challenges, what we can also do is not only have a previous architecture, but have progressive challenges. We will first implement one infrastructure, very minimal; then we will grow based on requirement. New challenges will appear, and those can fork. At some point, we might have requirements dedicated to better latency, and in other cases we have more challenges dedicated to multi-region or having observability or more requests per second, and so on. That is some other direction that we can take in order to have challenges that have already some architecture in place that we have to grow from. 

- Load-Balanced Edge Quest also falls in the category where we just do what it says in the first two lines. We don't even have to read the complete statement of the challenge, and with that we have the challenge done, nothing else to do. 

- Another thing that we can tackle here in the challenges in the quest that might be another field is that, for example, for compute I just use Python + Face API, which is incredibly more capable in terms of RPS than Python, Django, and Flask. I haven't even looked in Node.js, PHP, Go, C, C++, Ruby, or Java because I haven't had the need for an API for the price. It is super and handles everything.

On one hand, we have that set of configurations. Once we save it, it will one-shot many challenges. We might also have, in the challenges, some restrictions in terms of what vendors to use and configurations to use, based on the expertise of the team. Basically, you have a team that handles this or that. That way, we might be forced to use another version of the Python or configuration of the frameworks, another version of the Python frameworks, or another languages or vendors and technologies. We are not going to always have all the knowledge at hand, but we might have a team that is very fixed and works with certain tools. We might have to use those, and for those cases we might have to disable some vendors or some configurations that should also be part of the challenges.

I know it's a deeper change, but I think that it's worth the research and thinking through, making the analysis, and seeing how we can accommodate some of these tailored conditions into the challenges. 

- On the quest Zone Replica, in the end I had to change the database. I am using PostgreSQL, so I just had to change the situs distributed tier, but I don't understand what I did. I just changed an option, and I don't understand the trade-offs there or anything.

That's part of the information that we should communicate somehow to the user. There is cost involved, a lot more costs, right? Besides money, what else changes? Maybe complexity might be something else, and if that is the case, I think that maybe complexity should be added on the bar on the bottom where we show the different statistics, right, or somehow we should communicate that complexity goes up if that is the case. 

- Another thing that we should do also is that in the quest we should show in the toolbox all the blocks available for the user, all the blocks unlocked, because in the real world we won't have these scenarios where we have a limited number of options. We always are going to have everything that we know of up, and that's what we're trying to emulate.

Later, if we want to spend, there should be an option in the challenge, next to the hints or above the hints, somewhere around there, to filter and show only the required blocks. We shouldn't show the required blocks right away, because that also is information that gives hints about what to use to finish the quest. Instead, we will gatekeep that behind a paywall of Of stars, or in this case we might use the other currency that we were going to get when we resolved the challenges under stress conditions on the test conditions. When we break them, actually, let's make that instead of gaining an additional star when we may break the system architecture with three stars by modifying the traffic source options. Let's make that we want one of the other currencies that we are going to have; that will be the same currency that we will gain when we also make the architecture more able to handle different test conditions, like we mentioned before.

This new currency will be the one that we will use to unlock. If we want, for one unit of this currency on each unlock, this is the one that will allow us to see the required blocks in any given quest and that will also filter the blocks in the toolbox. If that doesn't happen, then we will show all the blocks that we have to build based on the progress of the user. 

- And again, some challenges are solved by brute force using a super high-tier vendor. We have two ways of blocking those:
1. By the general budget for the challenge
2. By just blocking a specific vendor
Instead of blocking, we should be able to unlock them. For some kinds of challenges, we probably will have to do both, because for some kinds of challenges we won't be able to have certain tools at hand due to, say, thing complexity or stack or compatibility. For others, we just don't know about them yet.

- By the way, of course that the challenges can block straight away, and we should show that ban blocks as options that shouldn't be used instead of just hiding it. In case that we need to block certain options, it makes it not available explicitly. For example, in the no cash no mercy quest, we should show the CDN and the cash blocks, but they should be blocked. 

- Also, for example, in the databases, the options that we have differ so much on the RPS. Is that correct? I would like to double verify that, if we find some discrepancies, we correct them and update the links and the descriptions. Remember that the tier description should also be there, and I would like to do that for all the other blogs too, since for some blog we have a super bazookas next to bow and arrow fields like that. The trade-off between those is not really clear besides the payment change. 

- On the polyglot persistence quest, if I put just an SQL database, the simulation doesn't mark anything wrong. I see at the end that the required blocks are missing, but I was thinking, for example, that the kind of traffic that is required for the NoSQL or the object storage or object storage containers and databases might be different from an SQL database. I think I wonder if there is some other kind of traffic or request that we might be missing, maybe in the compute level or the traffic source. Since we are going to have challenges that have architecture in place, we might be able to now configure this kind of divergence between data that goes into relational, data that goes into NoSQL, and data that must be in object storage, so I would like to explore options there also. 

- Actually, in the polyglot persistence quest, we have an issue because it says that it roots structured data into a relational database document to a NoSQL store and blobs to an object storage. If I put a blob object storage, it tells me at the end that it is a forbidden piece that shouldn't be there. Maybe some misconfiguration or something that we didn't expect is happening here. Actually, I just removed the object storage and got the three stars. If we detect an issue here, I want to make sure that we add this to the way that we configure quests and verify our resulting quest also against this criteria to see if it doesn't happen in the future. 

- 



