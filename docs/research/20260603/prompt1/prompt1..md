We are building a page similar to Code in Docs. I want you to check what we have on the screen. We have every possible component that we put in an architecture, so I want you to collect every detail that we have on those components. We are interested in:

the types of components

the type of lock

the name of the component

what they are supposed to do

the options that they have when we press on each one of the drop-downs

any links we can find on the internet that explain those or serve as vendor links for that specific option

any special functionality, like some of these might need others

 It seems like we have three cases:

The app server and web socket server need a load balancer in order to use replicas.

The workers need a queue in order to use replicas.