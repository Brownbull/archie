<!-- AUTO-GENERATED by scripts/gen-prompt-template.ts — DO NOT EDIT BY HAND. -->
<!-- Regenerate: npx tsx scripts/gen-prompt-template.ts -->

# Archie AI Prompt Template

Use this template to ask any AI (Claude, ChatGPT, etc.) to generate Archie-compatible architecture YAML, then bring the result in via **Import**.

---

## Instructions for AI

You are helping design a software architecture using Archie, a visual architecture simulator. Generate a YAML file that follows the Archie schema exactly.

**Rules:**
- `schema_version` must be `"4.0.0"`
- Every `component_id` MUST come from the Available Components tables below — unknown IDs import as un-scored placeholders
- Every `config_variant_id` MUST be one of the variants listed for that component
- Include at least one **Traffic Source** node (category below) — it is the demand the simulator drives load with
- On each traffic-source node, set `traffic_rps` (1–10,000,000), and optionally `traffic_kind` (`steady` | `realistic` | `periodic` | `search`), `traffic_workload` (`read` | `write` | `mixed`), and `traffic_origin` (`one-region` | `multi-region`)
- Node `id` and edge `id` values must be unique strings (e.g., `"n1"`, `"e1"`)
- Positions use pixel coordinates — space nodes at least 200px apart
- Edges connect nodes via `source_node_id` and `target_node_id` (direction = flow of requests)

---

## YAML Schema

```yaml
schema_version: "4.0.0"
name: "Optional architecture name"
nodes:
  - id: "n1"
    component_id: "<component-id>"      # from a table below
    config_variant_id: "<variant-id>"   # a variant of that component
    position: { x: 0, y: 0 }
    # traffic-source nodes only:
    traffic_rps: 1000
    traffic_kind: "realistic"
    traffic_workload: "mixed"
    traffic_origin: "multi-region"
edges:
  - id: "e1"
    source_node_id: "n1"
    target_node_id: "n2"
```

---

## Available Components (116 total)

### Traffic Sources (demand — every architecture needs at least one)

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `api-client` | API Client | `steady`, `burst` |
| `iot-sensors` | IoT Sensors | `fleet`, `large-fleet` |
| `mobile-users` | Mobile Users | `steady`, `peak` |
| `web-users` | Web Users | `moderate`, `heavy` |

### Delivery / Network

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `apigee` | Apigee | `standard`, `enterprise` |
| `aws-alb` | AWS ALB | `standard`, `with-waf` |
| `aws-api-gateway` | AWS API Gateway | `http-api`, `rest-api` |
| `azure-cdn` | Azure CDN | `standard`, `premium` |
| `azure-dns` | Azure DNS | `standard`, `private-zones` |
| `cloudflare-cdn` | Cloudflare CDN | `static-caching`, `full-site` |
| `cloudflare-dns` | Cloudflare DNS | `standard`, `steering` |
| `cloudfront` | AWS CloudFront | `standard`, `edge-functions` |
| `envoy` | Envoy Proxy | `edge`, `service-mesh` |
| `fastly-cdn` | Fastly CDN | `edge-caching`, `compute-edge` |
| `google-cloud-cdn` | Google Cloud CDN | `standard`, `premium` |
| `google-cloud-dns` | Google Cloud DNS | `standard`, `premium` |
| `haproxy` | HAProxy | `tcp-l4`, `http-l7` |
| `kong` | Kong Gateway | `oss`, `enterprise` |
| `nginx` | Nginx | `reverse-proxy`, `load-balancer` |
| `route53` | AWS Route 53 | `simple`, `latency-routing` |
| `traefik` | Traefik | `edge-router`, `load-balancer` |

### Compute

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `adyen` | Adyen | `standard`, `unified-commerce` |
| `airflow` | Apache Airflow | `self-managed`, `managed-mwaa` |
| `anthropic` | Anthropic Claude API | `balanced`, `fast` |
| `aws-bedrock` | AWS Bedrock | `on-demand`, `provisioned-throughput` |
| `aws-lambda` | AWS Lambda | `on-demand`, `provisioned-concurrency` |
| `bullmq` | BullMQ | `single-worker`, `scaled` |
| `celery` | Celery | `prefork`, `gevent` |
| `cloudflare-workers` | Cloudflare Workers | `standard`, `durable-objects` |
| `dbt` | dbt | `dbt-core`, `dbt-cloud` |
| `dotnet` | C# + ASP.NET Core | `standard`, `native-aot` |
| `etl-pipeline` | ETL Pipeline | `batch`, `micro-batch`, `streaming` |
| `fivetran` | Fivetran | `standard`, `enterprise` |
| `flink` | Apache Flink | `standalone`, `managed` |
| `gemini` | Google Gemini API | `balanced`, `fast` |
| `go-service` | Go Service | `single-binary`, `horizontal-pods` |
| `kafka-streams` | Kafka Streams | `standard`, `scaled` |
| `laravel` | PHP + Laravel | `fpm`, `octane` |
| `llm-gateway` | LLM Gateway | `single-model`, `dedicated-capacity`, `multi-model-router`, `streaming` |
| `node-express` | Node.js + Express | `single-process`, `cluster-mode` |
| `openai` | OpenAI API | `gpt-flagship`, `gpt-mini` |
| `payment-gateway` | Payment Gateway | `direct-integration`, `payment-orchestrator`, `recurring-billing` |
| `paypal` | PayPal / Braintree | `paypal-checkout`, `braintree-direct` |
| `python-django` | Python + Django | `wsgi-gunicorn`, `asgi-async` |
| `python-fastapi` | Python + FastAPI | `uvicorn`, `gunicorn-uvicorn` |
| `python-flask` | Python + Flask | `wsgi-gunicorn`, `gevent-async` |
| `rails` | Ruby on Rails | `puma-threaded`, `horizontal-pods` |
| `serverless` | Serverless Compute | `cold-start`, `provisioned-concurrency`, `edge-function` |
| `sidekiq` | Sidekiq | `standard`, `enterprise` |
| `spark-streaming` | Spark Structured Streaming | `micro-batch`, `continuous` |
| `spring-boot` | Java + Spring Boot | `standard-jvm`, `native-graalvm` |
| `stripe` | Stripe | `hosted-checkout`, `payment-intents` |

### Caching

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `memcached` | Memcached | `simple-cache`, `multi-node` |
| `redis-cache` | Redis Cache | `simple-cache`, `distributed-cache` |

### Data Storage

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `arangodb` | ArangoDB | `single-server`, `cluster` |
| `aws-aurora` | AWS Aurora | `provisioned`, `synchronous-multi-az`, `serverless-v2` |
| `aws-s3` | AWS S3 | `standard`, `intelligent-tiering` |
| `azure-blob` | Azure Blob Storage | `hot`, `cool` |
| `cassandra` | Apache Cassandra | `single-dc`, `multi-dc` |
| `cloudflare-r2` | Cloudflare R2 | `standard`, `enterprise` |
| `cockroachdb` | CockroachDB | `regional`, `multi-region` |
| `data-lake` | Data Lake | `raw-storage`, `lakehouse`, `managed-lake` |
| `dragonfly` | Dragonfly | `standard`, `scaled` |
| `dynamodb` | AWS DynamoDB | `on-demand`, `provisioned` |
| `elasticache` | Amazon ElastiCache | `standard`, `clustered` |
| `firestore` | Cloud Firestore | `standard`, `scaled` |
| `gcs` | Google Cloud Storage | `standard`, `nearline-coldline` |
| `google-cloud-sql` | Google Cloud SQL | `standard`, `ha-replica` |
| `graph-db` | Graph Database | `single-node`, `distributed-cluster`, `managed-cloud` |
| `influxdb` | InfluxDB | `oss`, `cloud` |
| `minio` | MinIO | `single-node`, `distributed` |
| `mongodb` | MongoDB | `replica-set`, `sharded` |
| `mysql` | MySQL | `single-node`, `primary-replica` |
| `neo4j` | Neo4j | `community-self-hosted`, `aura-managed` |
| `neptune` | Amazon Neptune | `provisioned`, `serverless` |
| `pinecone` | Pinecone | `serverless`, `pod-based` |
| `postgresql` | PostgreSQL | `single-node`, `primary-replica`, `citus-distributed`, `synchronous-replica` |
| `qdrant` | Qdrant | `self-hosted`, `cloud` |
| `redis` | Redis | `standalone`, `sentinel`, `cluster` |
| `timescaledb` | TimescaleDB | `single-node`, `multi-node` |
| `vector-db` | Vector Database | `single-node`, `distributed`, `managed-cloud` |
| `victoriametrics` | VictoriaMetrics | `single-node`, `cluster` |
| `weaviate` | Weaviate | `self-hosted`, `cloud` |

### Messaging / Streaming

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `amazon-mq` | Amazon MQ | `single-instance`, `active-standby` |
| `aws-kinesis` | AWS Kinesis | `on-demand`, `provisioned` |
| `aws-sqs` | AWS SQS | `standard-queue`, `fifo-queue` |
| `gcp-pubsub` | Google Pub/Sub | `standard`, `lite` |
| `kafka` | Apache Kafka | `single-broker`, `multi-broker` |
| `nats` | NATS | `core`, `jetstream` |
| `rabbitmq` | RabbitMQ | `single-node`, `clustered` |
| `redpanda` | Redpanda | `self-hosted`, `cloud` |

### Real-time

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `ably` | Ably | `standard`, `global-multi-region` |
| `phoenix` | Phoenix Channels | `standard`, `clustered` |
| `pusher` | Pusher | `public-channels`, `presence-channels` |
| `pushpin` | Pushpin | `standard`, `scaled` |
| `socketio` | Socket.IO | `single-node`, `redis-adapter` |
| `websocket-server` | WebSocket Server | `single-server`, `clustered` |

### Search

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `algolia` | Algolia | `standard`, `pro` |
| `elasticsearch` | Elasticsearch | `self-hosted`, `managed` |
| `meilisearch` | Meilisearch | `standard`, `scaled` |
| `opensearch` | OpenSearch | `standard`, `optimized` |
| `typesense` | Typesense | `standard`, `ha-cluster` |

### Auth / Security

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `auth0` | Auth0 | `standard`, `enterprise` |
| `firebase-auth` | Firebase Authentication | `standard`, `identity-platform` |
| `keycloak` | Keycloak | `single`, `clustered` |
| `rate-limiter` | Rate Limiter | `token-bucket`, `sliding-window` |

### Monitoring / Observability

| Component ID | Name | Config Variant IDs |
|---|---|---|
| `aws-waf` | AWS WAF | `standard`, `advanced` |
| `azure-firewall` | Azure Firewall | `standard`, `premium` |
| `cloudflare-waf` | Cloudflare WAF | `standard`, `bot-and-rate` |
| `datadog` | Datadog | `apm`, `full-stack` |
| `grafana` | Grafana Stack | `self-hosted`, `cloud` |
| `newrelic` | New Relic | `standard`, `full-platform` |
| `prometheus` | Prometheus | `standalone`, `federated` |
| `siem` | SIEM | `on-premise`, `cloud-native`, `hybrid` |
| `splunk` | Splunk | `enterprise-self-hosted`, `splunk-cloud` |
| `vault` | HashiCorp Vault | `self-hosted`, `hcp-vault` |

---

## Complete Example (AI Chat Service)

```yaml
schema_version: "4.0.0"
name: "AI Chat Service"
nodes:
  - id: "n1"
    component_id: "web-users"
    config_variant_id: "moderate"
    position: { x: 0, y: 200 }
    traffic_rps: 5000
    traffic_kind: "realistic"
    traffic_workload: "mixed"
    traffic_origin: "multi-region"
  - id: "n2"
    component_id: "cloudflare-cdn"
    config_variant_id: "full-site"
    position: { x: 240, y: 200 }
  - id: "n3"
    component_id: "node-express"
    config_variant_id: "cluster-mode"
    position: { x: 480, y: 200 }
  - id: "n4"
    component_id: "llm-gateway"
    config_variant_id: "single-model"
    position: { x: 720, y: 200 }
  - id: "n5"
    component_id: "redis"
    config_variant_id: "standalone"
    position: { x: 480, y: 420 }
edges:
  - id: "e1"
    source_node_id: "n1"
    target_node_id: "n2"
  - id: "e2"
    source_node_id: "n2"
    target_node_id: "n3"
  - id: "e3"
    source_node_id: "n3"
    target_node_id: "n4"
  - id: "e4"
    source_node_id: "n3"
    target_node_id: "n5"
```

---

## Prompt to Use

Copy and paste the following into your AI chat:

> "Generate an Archie-compatible YAML architecture (schema_version 4.0.0) for [describe your use case]. Use ONLY component IDs and config_variant_IDs from the Available Components tables. Include at least one traffic source with a traffic_rps. Follow the schema exactly. Return only the YAML, no explanation."
