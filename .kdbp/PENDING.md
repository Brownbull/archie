# Deferred Items

| # | Date | Source | Finding | File | Scale | Priority | Impact | Times Deferred | Status |
|---|------|--------|---------|------|-------|----------|--------|----------------|--------|
| D1 | 2026-06-02 | user-feedback | Port handle dots lack hover tooltips — colored dots on node edges don't communicate port type/direction (e.g., "HTTP Out", "Database In"). Users can't tell what a port is for without connecting to it. | src/components/canvas/ArchieNode.tsx | enterprise | medium | moderate | 0 | resolved |
| D2 | 2026-05-29 | gabe-execute | architectureStore.test.ts is 1454 lines (>800 size guard) — blocks Edit-tool changes, forces Write/sed workarounds. Split into focused files (addNode, edges, variants, etc.) mirroring the architectureStore-*.test.ts convention. | tests/unit/stores/architectureStore.test.ts | enterprise | medium | moderate | 0 | resolved |
| D3 | 2026-05-29 | gabe-review | architectureStore.ts crossed the 800-line size guard (815 lines) with Epic 14 Phase 1 additions — now blocks Edit-tool changes. Extract concerns (e.g. replica/economics actions, recalculation orchestration, hydration) into focused modules or store slices. | src/stores/architectureStore.ts | enterprise | medium | moderate | 0 | resolved |
| D4 | 2026-05-29 | gabe-execute | Pre-existing: ComponentDetail.tsx has 6 eslint react-hooks/refs errors (previousVariantIdRef.current read during render, Epic 4 delta pattern). Not gating (commit gate=biome, CI=tsc+vite) but real. Refactor the previous-variant delta to a state/derived value, not a render-phase ref read. | src/components/inspector/ComponentDetail.tsx | enterprise | low | low | 0 | resolved |
| D5 | 2026-05-29 | gabe-review | Pre-existing stale E2E specs: several specs (canvas-and-placement, connection-wiring, port-handles, status-dot, etc.) assert generic handles `archie-node-handle-target/source` which Epic 12's typed-port system replaced with `port-handle-*` for port-equipped components. ~36 e2e failures in full parallel run (mix of these stale specs + sandbox contention). E2E is not in the CI deploy gate. Update specs to the port-handle model + consider workers:1 locally. | tests/e2e/ | enterprise | medium | moderate | 0 | resolved |
| D6 | 2026-05-29 | gabe-review | ArchieNode.test.tsx exceeds the 800-line size guard after Epic 14 Phase 3 replica tests. Split into focused files mirroring the per-feature test convention. | tests/unit/components/canvas/ArchieNode.test.tsx | enterprise | low | low | 0 | resolved |
| D7 | 2026-05-29 | gabe-review | Simulation engine: flow accounting is approximate for DAG-feeds-into-cycle topologies — a cycle member's served traffic neither forwards nor reaches a sink, so totalServedRps overcounts. Conserves correctly for pure DAGs (the normal case). Refine cycle handling if a real architecture needs traffic cycles (e.g. retry loops). | src/engine/simulationEngine.ts | enterprise | low | low | 0 | resolved |
| D8 | 2026-05-29 | gabe-push | CI deploy workflow uses Node-20 actions (actions/checkout@v4, actions/setup-node@v4, FirebaseExtended/action-hosting-deploy@v0). GitHub forces Node 24 from 2026-06-02 (~4 days). checkout/setup-node v4 support node24, but action-hosting-deploy@v0 may not — could break the production deploy gate. Bump action versions or pin FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 before the cutoff. | .github/workflows/ | enterprise | high | high | 0 | resolved |
| D9 | 2026-05-29 | gabe-execute | Firestore `attempts` owner-only rules added to firestore.rules but NOT deployed (CI deploys hosting only via action-hosting-deploy). Run `firebase deploy --only firestore:rules` (needs Firebase creds) so attempt persistence is enforced + functional. Until deployed, attempt writes are denied (recordAttempt is resilient, so no UX break — attempts just don't save). | firestore.rules | enterprise | high | high | 0 | resolved |
| D10 | 2026-05-29 | gabe-execute | Optional brand-logo polish (Epic 17 roadmap): add `brand`/`logoUrl` to ConfigVariant + render a logo on variant cards/nodes. Deferred (D37) — needs curated per-variant logo assets + an https-only URL-validation security pass (no javascript: URIs). Low value without seeded brand data. | src/schemas/componentSchema.ts | enterprise | low | low | 0 | resolved |
| D11 | 2026-05-30 | gabe-execute | Residual E2E flakiness (environmental, not in CI gate). P5/P3/P6 spec-drift RESOLVED (inspector disclosures, provider-swap label, pane-click target, delete-node, tour baseline — suite 11→4). Remaining 4 are the original flaky cluster: mouse-drag-to-connect is intermittent (component-swapping AC-2, connection-wiring AC-4, settings:284) + inspector aside not collapsing to 0 width on pane-deselect (inspector:295). Stabilize drag helper + width-collapse. | tests/e2e/ | enterprise | low | low | 0 | open |
| D12 | 2026-05-30 | gabe-review | loadAttempts fetches ALL of a user's attempts and sorts client-side (no limit). Fine at single-player scale; bounding it needs orderBy(createdAt).limit(N) which reintroduces the composite-index deploy dependency that P4 removed. Revisit (composite index + limit, or paginate) only if a user's attempt count grows large. | src/stores/attemptsStore.ts | enterprise | low | low | 0 | open |
| D14 | 2026-05-31 | gabe-review | ComponentSchema/ComponentYamlSchema are .strict(), so they reject unknown keys — forward-incompatible: a re-seed with a new field breaks already-deployed older readers (hit during P5 typeId). Consider a non-strict/passthrough runtime reader schema so additive fields never break old deployments. | src/schemas/componentSchema.ts | enterprise | medium | moderate | 0 | resolved || D15 | 2026-06-05 | gabe-execute | ED3 (behavior-over-presence): required_types must be ON the served path / carry traffic, not just present on canvas. Rescoped from Phase 1 → Phase 3 (D76): a safe sync/async exemption needs the builder's on-path classification (worker/stream-processor are category `compute` but wired off-path; only messaging/real-time are explicitly off-path), which Phase 3 reworks via ED2 fractional-AZ outage. Implement alongside Phase 3 resilience so the harness stays 3★. | src/engine/rubricScorer.ts | enterprise | high | high | 1 | resolved |
| D16 | 2026-06-05 | workflow | ED5 (dynamic cache hit-ratio) infeasible as specified: reference builder never sets trafficWorkload → writePressure=0.5 derates every cache in all 57 builds; trafficKind never reaches the harness. Needs workload-source plumbing + a 3-step bisectable rollout (derating disabled → live-canvas-only → per-challenge opt-in) before it can ship. | src/engine/simulationEngine.ts | enterprise | medium | moderate | 0 | resolved |
| D17 | 2026-06-05 | workflow | 40-observe-to-recover becomes structurally unclearable once EN7 removes MONITORING_RECOVERY_FACTOR (max uptime 66.7% < 80% target, palette lacks cdn/cache). Redesign BEFORE EN3/EN7: add fronting blocks, or a multi-compute+breaker topology, or lower the target + rewrite the brief. Also its component_failure target is a category string matching no node id. | src/data/challenges/40-observe-to-recover.yaml | enterprise | high | high | 0 | resolved |
| D18 | 2026-06-06 | gabe-execute | EN2 part B AUTHORING (concurrency_limit on variants + a pool-exhaustion challenge) — deferred as a focused calibration pass. The concurrency-aware builder (W_est 5.5× = ρ≈0.91) is landed (d82baee); what remains is the delicate authoring. KEY FINDING: at the lean build's ρ≈0.91 the ED4 curve inflates W ≈5.5× base, so gate in-flight = served × base×5.5/1000 is already high. The 5 LLM challenges share llm-gateway with per-replica in-flight ≈ llm-service 162 / rag-retrieval 154 / data-pipeline 261 / production-ai 80 / the-singularity 229 (streaming@50ms is a weak concurrency vehicle; single-model@500ms is the strong one). A binding limit must sit just above existing in-flight but below a new challenge's — a tight window. Recommended: add a NEW high-latency llm variant the new challenge uses (existing challenges keep the cheaper variants), OR author on single-model/multi-model + verify all 5 LLM challenges stay 3★ within budget + MAX_REPLICAS. | src/data/components/llm-gateway.yaml | enterprise | medium | moderate | 0 | open |
| D19 | 2026-06-06 | gabe-execute | ED9 in-game lesson (autoscale cost) needs a builder change to LAND. The cost MECHANIC ships faithful + unit-proven (autoscaleCost.test): an autoscale tier bills mean-active over the curve. But the reference builder ALWAYS places a generic static `compute` tier (referenceSolution.ts:211 `wanted=new Set(["traffic-source","compute"])`), so any serverless challenge carries a static tier whose flat cost dwarfs the serverless autoscale discount (burst-economics probe: flat $620 / integrated $591 — only 5%). To land a serverless-primary burst challenge where the discount dominates: change the builder to skip generic `compute` when a compute-category required_type (e.g. serverless) is present, then re-tune serverless-burst + cold-start-spike (they'd drop their redundant compute tier → cheaper → re-tune par). Then author the high-burst R>1 challenge (spiky curve, NOT the `periodic` kind which stays near-peak). | tests/integration/challenges/referenceSolution.ts | enterprise | medium | moderate | 0 | resolved |
| D20 | 2026-06-06 | gabe-execute | ED5 `cacheable_fraction` is wired (schema + engine + harness blend, unit-tested) but NO challenge authors it yet, and the LIVE canvas traffic node isn't seeded with it (unset⇒1). To ship a dedicated "uncacheable workload" lesson (e.g. personalized feed / IoT telemetry, cacheable_fraction ~0.3 → a cache barely helps → size the origin for real load): (1) seed cacheableFraction onto the canvas traffic node when a challenge starts (mirror how trafficWorkload/trafficKind are seeded — find the challenge→canvas demand hook), (2) add architectureFileSchema.trafficSource.cacheableFraction for round-trip, (3) author the challenge + verify 3★ + live==harness. write-pressure + erosion already teach dynamic hit-ratio across all 59; this is the explicit cacheability showcase. | src/data/challenges/ | scale | low | low | 0 | resolved |
| D21 | 2026-06-07 | gabe-execute | Desktop-project E2E validation pass after Fluidity P1+P2 (NOT CI-gated — CI runs only vitest + desktop-unlocked — and the desktop project isn't runnable in this env, so these are tsc-validated + reviewed but not runtime-verified). (a) P1: 12 desktop specs + the config-recalc helper were redirected from the removed inspector tuners (component-swapper / config-selector / inspector-traffic-config) to the on-node controls (archie-node-provider / archie-node-config-trigger). (b) P2: scoring-dashboard.spec asserts the full set of FOOTER category-bar-* across ~10 tests (count / mean / role=meter / fill-transition), but the footer now shows only the WEAKEST category (testid dashboard-weakest) + an expand button; the full breakdown moved to the overlay under DIFFERENT testids (overlay-category-*, not category-bar-*, and overlay tiles aren't role=meter). Rework: footer assertions → dashboard-weakest only; full-set assertions → openDashboardOverlay + overlay-category-*. Then run `playwright test --project=desktop` to validate the whole batch. | tests/e2e/scoring-dashboard.spec.ts | enterprise | medium | moderate | 0 | resolved |
| D22 | 2026-06-07 | gabe-execute | Desktop-project E2E rehab [PROGRESS 2026-06-07: scoring-dashboard.spec REHABBED + validated green on desktop (11 passed / 0 failed) via a reusable useAdvancedLevel helper (advanced level + canvas-autosave clear for isolation), add-type-{typeId} placement (the per-vendor add-to-canvas- cards aren't in the default toolbox view), footer 'mean'→weakest-inline rework, deterministic inspector-remove-node delete, and scoping the tier-badge assertion to aria-controls (the always-present ⓘ PanelInfoButton is also aria-expanded). RESIDUAL still open (deep pre-existing app-evolution rot, low ROI): inspector-and-config (app now auto-selects newly-placed nodes → breaks AC-1's 'inspector hidden before selection'; ~5 disclosure/collapse tests; the localStorage advanced-seed didn't resolve gating in practice — needs the UI level control or per-test rework), component-types type-group-* (testid renamed in the toolbox redesign), vendor-links NEW vendors (Traefik/Azure CDN/Cloudflare R2 absent from the seeded test-Firebase 'components' collection → needs a re-seed)]. DORMANT, NOT CI-gated (CI runs only vitest + desktop-unlocked). A focused validation pass found the desktop suite has EXTENSIVE pre-existing rot independent of Fluidity (confirmed by running the pre-Phase-1 spec: AC-1/AC-4/delete failed identically): (1) default experienceLevel is "beginner" so inspector progressive-disclosure sections (disclosure-gains/costs/metrics) are hidden, failing tests that assert them (inspector-and-config AC-1/AC-3/AC-5/code-snippet/metric-explanation) — these tests must raise the level to advanced first; (2) toolbox placement via `add-to-canvas-{componentId}` cards isn't visible in the default type-block view (`add-type-{typeId}`), failing scoring-dashboard placeComponent + component-types type-group-* structure tests; (3) inspector collapse/delete tests fail; (4) new vendors (Traefik / Azure CDN / Cloudflare R2) aren't in the seeded test-Firebase data (seed drift) → vendor-links NEW-vendor tests. PLUS the Fluidity P2 follow-up: scoring-dashboard footer assertions (count/mean-of-visible/role=meter) need rework for the slim footer (footer=weakest only via dashboard-weakest; full set via the overlay's overlay-category-* tiles, which are NOT role=meter). Needs a dedicated pass with the desktop env runnable. | tests/e2e/scoring-dashboard.spec.ts | enterprise | medium | moderate | 0 | resolved |
| D23 | 2026-06-07 | gabe-execute | Remaining DORMANT desktop E2E specs (not CI-gated; surfaced/touched during Fluidity P3a). (a) connection-inspection.spec — 7/9 fail on PRE-EXISTING rot, NOT a P3a regression: setupConnectionAndSelect can't create an edge (`.react-flow__edge` count 0) + uses raw `.react-flow__pane` clicks — same class as the D22 family (edge-creation/drag + pane-deselect). Rehab with the D22 playbook (useAdvancedLevel, node-header safe-click, Escape deselect, deterministic edge creation). (b) data-context / ui-sweep-edge / inspector-ux-polish — their `inspector-section-nav` references were updated (the nav was removed in P3a) to scroll to the sections directly / drop the obsolete nav assertion; tsc-validated but NOT runtime-verified, and they likely carry the same dormant beginner-gating rot. Batch with a desktop-rehab pass. | tests/e2e/connection-inspection.spec.ts | enterprise | medium | moderate | 0 | open |
